server:
  port: 8080
spring:
  main:
    banner-mode: off
  application:
    name: account-service
  datasource:
    url: jdbc:postgresql://192.168.50.22:5432/accounts_service_db
    username: acc
    password: acc
  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update

